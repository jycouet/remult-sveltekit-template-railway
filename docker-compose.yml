services:
 db:
  image: postgres:17-alpine
  restart: unless-stopped
  environment:
   POSTGRES_PASSWORD: postgres
   POSTGRES_DB: app
  ports:
   - '5432:5432'
  volumes:
   - db:/var/lib/postgresql/data

volumes:
 db:
